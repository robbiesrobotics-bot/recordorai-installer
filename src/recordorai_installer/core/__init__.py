"""Headless installer core — no UI, no I/O surprises.

The :mod:`detect` / :mod:`plan` / :mod:`exec_` / :mod:`license` /
:mod:`uninstall` submodules are the entire business logic. Both the
TUI and the Tauri front-ends drive this surface.
"""
